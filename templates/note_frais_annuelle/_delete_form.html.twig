<form method="post" action="{{ path('note_frais_annuelle_delete', {'id': note_frais_annuelle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note_frais_annuelle.id) }}">
    <button class="btn">Delete</button>
</form>
