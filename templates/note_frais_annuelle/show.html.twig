{% extends 'base.html.twig' %}

{% block title %}NoteFraisAnnuelle{% endblock %}

{% block body %}
    <h1>NoteFraisAnnuelle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note_frais_annuelle.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ note_frais_annuelle.annee }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ note_frais_annuelle.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('note_frais_annuelle_index') }}">back to list</a>

    <a href="{{ path('note_frais_annuelle_edit', {'id': note_frais_annuelle.id}) }}">edit</a>

    {{ include('note_frais_annuelle/_delete_form.html.twig') }}
{% endblock %}
