{% extends 'base.html.twig' %}

{% block title %}NoteFraisMois{% endblock %}

{% block body %}
    <h1>NoteFraisMois</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note_frais_moi.id }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ note_frais_moi.mois }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ note_frais_moi.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('note_frais_mois_index') }}">back to list</a>

    <a href="{{ path('note_frais_mois_edit', {'id': note_frais_moi.id}) }}">edit</a>

    {{ include('note_frais_mois/_delete_form.html.twig') }}
{% endblock %}
