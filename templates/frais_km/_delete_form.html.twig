<form method="post" action="{{ path('frais_km_delete', {'id': frais_km.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais_km.id) }}">
    <button class="btn">Delete</button>
</form>
