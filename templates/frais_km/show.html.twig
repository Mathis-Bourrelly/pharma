{% extends 'base.html.twig' %}

{% block title %}FraisKm{% endblock %}

{% block body %}
    <h1>FraisKm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frais_km.id }}</td>
            </tr>
            <tr>
                <th>Distance</th>
                <td>{{ frais_km.distance }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ frais_km.cv }}</td>
            </tr>
            <tr>
                <th>Depart</th>
                <td>{{ frais_km.depart }}</td>
            </tr>
            <tr>
                <th>Arrivee</th>
                <td>{{ frais_km.arrivee }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ frais_km.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('frais_km_index') }}">back to list</a>

    <a href="{{ path('frais_km_edit', {'id': frais_km.id}) }}">edit</a>

    {{ include('frais_km/_delete_form.html.twig') }}
{% endblock %}
